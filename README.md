# Simple node compressor

A very simple brotli and gzip compress that requires no build tools to use.

# Options

```
Usage: compress [options]

Options:
  -f, --folder [folder]
  -h, --help             output usage information
```
